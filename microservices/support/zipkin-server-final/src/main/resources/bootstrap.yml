spring:
  cloud:
    config:
      name: zipkin
      discovery:
        service-id: config
        enabled: true
        username: configUser
        password: configPassword

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/
    register-with-eureka: true
    fetch-registry: true